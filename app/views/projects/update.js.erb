$('.projects #project-<%= @project.id %> .project-errors').html('<%= render 'shared/errors', resource: @project%>');

<% if !@project.errors.present? %>
  $('.projects #project-<%= @project.id%> ').html('<%= j render @project %>');
<% end %>
