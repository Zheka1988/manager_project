$('.tasks #task-<%= @task.id %> .task-errors').html('<%= render 'shared/errors', resource: @task%>');

<% if !@task.errors.present? %>
  $('.tasks #task-<%= @task.id%> ').html('<%= j render @task %>');
<% end %>
